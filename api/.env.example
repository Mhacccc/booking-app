# Database Configuration
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/bookingApp

# Server Configuration
PORT=3000

# Security (JWT) Configuration
# You can generate a secure random secret using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your_jwt_secret_key_here
